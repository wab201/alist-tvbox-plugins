# 豆瓣 TMDB 追更助手 V70 到 V95 逐版本更新说明

发布日期：2026-08-23

V90 将 V80-V90 私有迭代线中的架构、同步、追更、预热、身份和候选源能力正式晋升为公开版本。V91 继续在私有模块化开发线补齐刷新、去重和预热审计，V92 将资源身份分层验证与用户可见预热状态晋升为公开版本，V93 收口运行时自动识别、声明式 Web 配置和独立拦截器刷新说明，V94 收紧候选的详情信任边界并修复云盘分集与容量解析，V95 将 PanSou 纳入详情信任路径并收紧最终详情的身份回写。本文按真实版本产物记录变化；没有形成独立产物的版本会明确标注，不补造不存在的发布。

## 2026-08-23：V95 追更拦截器独立产物

- 新增 `filters/DoubanTmdbFollow.py`，从 V95 已验证的追更拦截器逻辑抽取为只导出 `Filter` 的独立文件。
- 根 `spiders_v2.json` 保持只声明主插件；过滤器管理改用独立 URL 刷新，不再复用带 `Spider` 的主插件产物。
- 不改变 V95 的 PanSou 详情信任、独立播放组或追更条目 ID 回写行为。

## 2026-08-21：通用番号中文字幕过滤器 V3

- 新增 `filters/UniversalNumberSubtitle.py`，只导出 `Filter`，不加入根 `spiders_v2.json`。
- 按 AList-TVBox 官方 `1.53.0` 源码契约，主插件仓库导入与过滤器 URL 管理保持两条独立链路。
- 从详情与播放上下文识别番号，使用 FongMi 原生 `subs` 追加中文字幕，保留并去重已有字幕。
- 支持 `parse=0` 和 `parse=1` 播放返回；外部 URL 只接受 HTTP/HTTPS，HLS Worker 仅包装 `.m3u8`。
- MissAV 主插件撤出公开树，改为容器本地 `static/plugins/` 私有部署。

## 版本状态总览

| 版本 | 日期 | 发布状态 | 主要主题 |
| --- | --- | --- | --- |
| V70 | 2026-08-13 | 公开测试版，现为历史标签 | 完整线路首开与固定公开入口 |
| V71 | - | 未形成独立产物 | 无独立更新 |
| V72 | - | 未形成独立产物 | 无独立更新 |
| V73 | - | 未形成独立产物 | 无独立更新 |
| V74 | - | 未形成独立产物 | 无独立更新 |
| V75 | - | 未形成独立产物 | 无独立更新 |
| V76 | - | 未形成独立产物 | 无独立更新 |
| V77 | - | 未形成独立产物 | 无独立更新 |
| V78 | - | 未形成独立产物 | 无独立更新 |
| V79 | - | 未形成独立产物 | 无独立更新 |
| V80 | 2026-08-18 | 私有冻结基线 | 模块化重构、可靠性、安全和可观测性 |
| V81 | 2026-08-19 | 私有迭代 | 播放状态 v2 与追更进度模型 |
| V82 | 2026-08-19 | 私有迭代 | 快速同步、交互反馈和资源名清洗 |
| V83 | 2026-08-19 | 私有迭代 | 预热线路持久化与过滤器 owner |
| V84 | 2026-08-19 | 私有迭代 | 恢复六 owner 模块化构建 |
| V85 | 2026-08-19 | 私有迭代 | 失败重试收敛与取消追更入口调整 |
| V86 | 2026-08-19 | 私有迭代 | 全量主动预热、进度和可播放状态 |
| V87 | 2026-08-19 | 私有迭代 | 受控订阅插件候选扩源 |
| V88 | 2026-08-19 | 私有迭代 | 服务端 TMDB 回退和安全缓存 |
| V89 | 2026-08-20 | 私有迭代 | 临时剧集身份与后补录迁移 |
| V90 | 2026-08-20 | 历史公开正式版 | 选择性 TMDB 双链路热备 |
| V91 | 2026-08-20 | 私有模块化迭代 | 缓存合并、线路失败分类、预热排序、History 去重与淘汰审计 |
| V92 | 2026-08-20 | 历史公开正式版 | 网盘资源身份分层验证与追更卡片预热状态 |
| V93 | 2026-08-21 | 历史公开正式版 | 运行时自动识别、声明式 Web 配置与拦截器双刷新链 |
| V94 | 2026-08-23 | 历史公开正式版 | 详情信任边界与云盘标签解析 |
| V95 | 2026-08-23 | 当前公开正式版 | PanSou 详情信任、独立播放组与追更身份回写 |

## V70：公开测试基线

### 首开和线路覆盖

- 追更详情预热只有在单条绑定线路连续覆盖 E01 到当前目标集时，才允许直接返回为完整可播线路。
- 旧绑定只有最近单集时，继续执行受限前台候选搜索，不让单集绑定阻断完整季度资源。
- 完整候选未及时返回时，旧线路仍作为有界超时兜底，不把可播放能力直接清空。
- 增加“旧绑定仅 E06、候选覆盖 E01-E06”的首开回归场景。

### 发布结构

- 公开入口固定为 `py/豆瓣TMDB追更单入口.py`，停止为每次升级复制一份带版本号的完整脚本。
- 插件 ID 保持 `douban_tmdb_follow_single`，AList-TVBox 可通过递增版本刷新同一插件。
- 当时的回退依赖 Git 提交或标签；在 V90 公开后，V70 只保留为历史标签，不再参与运行时回退。

## V71-V79：未发布区间

V71、V72、V73、V74、V75、V76、V77、V78、V79 没有形成独立源码包、部署记录或可核验发布身份。后续开发直接进入 V80 重构线，因此这些编号不承载功能变化。

## V80：生产化重构基线

V80 是后续版本的冻结架构基线，不是简单功能补丁。

### 开发与发布模型

- 将开发态拆为资源模型、Provider、Schema、标题规范化、匹配、评分、合并、排序和输出准入等明确职责。
- 开发态采用模块化来源，运行时继续生成 AList-TVBox/FongMi 可加载的单个 Python 文件。
- 建立输入指纹、构建身份和依赖闭包；输入未变化的通过证据可以复用，失败后按依赖图只续跑失效节点。

### 播放同步

- 对接 AList-TVBox `/api/playback/*`，支持增量游标、完整快照、墓碑、导入提交和 UID 重新识别。
- 兼容旧 History 通道的受控回退，并接入多级续播坐标和网盘身份字段。
- History、播放和搜索分别拥有自己的并发与生命周期，旧任务不能覆盖新配置产生的状态。

### 资源候选

- 候选行经过规范化、身份合并、Provider 识别、质量评分、稳定排序和最终输出准入。
- 保留原始行信息，避免分层重构时丢失网盘类型、分享身份、标题来源和播放句柄。
- 搜索与播放使用有界队列、绝对超时预算、熔断、健康状态和失败隔离。

### 缓存和后台任务

- 响应缓存支持过期可用、后台刷新和失败退避。
- 后台任务按搜索、播放、History 和资源补全分舱，避免一个慢源占满所有执行能力。
- 增加生命周期稳定性、冷热缓存、长时间资源增长和并发所有权验证。

### 安全边界

- JSON 和 HTML 响应在解析前限制大小，并限制结构深度、集合规模、键和值长度。
- 媒体地址逐跳校验 DNS、重定向、HTTPS 降级和跨域请求头。
- 自动重试只保留一个明确 owner；写操作、鉴权和状态变更不进入模糊自动重试。
- 诊断信息统一脱敏 Cookie、Token、密码、签名地址和订阅参数。

### 可观测性

- 增加有界诊断事件和快照，只保存阶段、计数、错误类型和脱敏上下文。
- 诊断返回使用脱离内部缓冲区的副本，避免读取诊断时反向污染运行状态。

### 上游兼容

- AList-TVBox 合同最终推进到 `1.51.1 / 47432df300c1ee54e799fe9c7a3eb169823c2f0e`。
- 保持 ATVP raw Python、直接 FongMi Python、详情、播放和分类参数双运行时兼容。

## V81：播放状态 v2 与追更模型

### 播放记录

- 播放状态升级为 v2，保存完成状态、季集导航、网盘身份、播放资源身份和远端更新时间。
- v1 数据保持只读迁移兼容，不要求用户清空已有播放记录。
- 已看水位只允许前进；重看较低集可以更新当前续播位置，但不能降低已经确认的已看集数。
- 迟到墓碑只清除续播位置、路线和附加播放状态，不删除更新后的追更水位。

### 身份冲突

- 强身份冲突不再依靠标题强行覆盖，改为显示“播放记录待确认”。
- 播放记录和追更身份同时考虑 TMDB、季集、网盘和资源身份。

### 追更动态

- 追更动态改为全部条目统一排序后再分页。
- 统一“更新至 / 已看 / 正在看 / 继续观看 / 播放下一集”等简体中文状态。
- 清理播放记录只影响续播和播放记录，不取消追更，也不回退已看水位。

## V82：快速同步与交互反馈

### 快速同步

- 使用 AList-TVBox 播放进度增量通道执行轻量同步，避免每次进入页面都执行完整 History 合并。
- 播放结束、手动同步和页面轻量刷新分别使用适合的同步路径。
- 较新的本地进度继续受保护，空云端数据不能覆盖有效本地记录。

### 追更交互

- “检查同步连接”等操作增加可持续查看的运行中、成功和失败状态，不再只有短暂提示。
- 追更确认、管理和动态页面使用一致的动作结果卡。

### 候选匹配

- 增加 AList 风格资源名清洗，去除年份、季集、画质、编码、网盘和发布装饰后再匹配。
- 放宽有明确剧集证据的标题匹配，修复《沧元图》等项目无法进入可信候选的问题。
- 收敛多余选集占位和重复播放提示，保留真正可点击的集数。

### 线路恢复

- 实际播放成功的线路写入可恢复绑定。
- 详情打开时优先复用已验证路线，失效后再触发候选替换。

## V83：预热线路持久化与过滤器 owner

- 后台线路预热通过媒体 Range 验证后，不再只写短期探测缓存，而是立即写入安全长期绑定。
- 持久化仅保存资源 ID、播放句柄、季、集、Provider 和质量摘要。
- 签名 URL、Cookie、Token、完整播放器响应和临时鉴权参数不进入长期状态。
- 写入前核对配置代次和后端身份，旧 worker 不能覆盖新配置。
- 标题规范化和资源名清洗形成独立过滤职责，并校验其与发布单文件中的运行方法保持一致。

## V84：恢复六 owner 模块化构建

- 停止继续直接扩改单文件。
- 将开发职责固定为：版本元数据、追更交互、候选识别、播放记录合并、线路预热与恢复、播放列表输出。
- 资源名过滤与规范化保持独立职责。
- 冻结 V80 基线叠加 owner 生成唯一运行时单文件；canonical、私有 staging 和公开脚本全部降级为构建产物。
- V81-V83 历史包只用于发布谱系取证，不反向拆分或覆盖当前 owner。
- 后续验证按变化 owner 和依赖传播执行，不恢复旧十八步日常门禁或无变化全量测试。

## V85：失败重试收敛与取消追更调整

### 确认失败

- 追更确认失败后记录候选标题、失败状态和更新时间。
- 同一候选在 30 秒冷却期内重复触发时，直接提示“已停止自动重试”，不再启动新的确认任务。
- 用户可以在冷却结束后手动重试；失败不会造成页面无限刷新或后台无限调用。

### 取消追更

- 删除顶级“取消追更”独立分类。
- 取消操作下沉到“追更管理”的管理模式中，避免占用一个长期分页入口。
- 页面刷新目标同步移除旧独立分类，避免客户端继续刷新不存在的页面。

## V86：全量主动预热与可播放进度

### 入口和进度

- 主动预热卡同时出现在追更管理和追更动态首屏，解决入口难以发现的问题。
- 状态明确显示总数、已处理数量、可直接播放数量和正在验证的线路数。
- 完成后显示“预热完成”，并区分全部可播与仍未找到有效线路的条目。

### 全追更调度

- 点击主动预热后遍历全部有效追更条目，而不是只启动当前一小批后台任务。
- 使用有界并发和待处理队列，任务完成后自动补入下一批，直到所有条目均被处理。
- 同一身份去重；已经具有有效长期绑定或现成可播资源的条目直接计为完成。
- 单个条目失败会计入未就绪结果，但不会阻断其他条目继续预热。

### 可播放绑定

- 可直接播放状态要求资源 ID、播放句柄、季集、后端身份和当前 History 集数一致。
- 线路探测缓存已有可达结果时，立即转为长期成功绑定，不重复发起相同探测。
- 资源预热结束后继续执行当前剧集线路预热，并及时刷新追更分类状态。

## V87：受控订阅插件候选扩源

- 当 AList `/vod`、`/vod1`、PanSou 和 Telegram 候选不足或不能覆盖目标集时，从当前活动订阅的受控插件白名单补充候选。
- 排除追更助手自身及同谱系插件，避免递归搜索。
- 每个插件使用独立有限任务、固定超时和失败隔离；一个插件异常不阻断其他来源。
- 插件搜索结果仍需通过标题评分、详情转换、播放器返回、安全输出、媒体 Range 和目标集覆盖验证。
- 只有验证成功的封装资源 ID 和数字播放句柄可以进入缓存或长期绑定。
- 原始插件播放 ID、分享 URL、签名 URL 和认证字段只保留在短期运行上下文。

## V88：服务端 TMDB 回退与安全缓存

- 客户端 TMDB 请求发生 TLS、连接或传输错误时，允许通过 AList-TVBox `/api/tmdb/meta` 通道回退。
- 服务端结果必须满足电视剧可信度和现有电影排除规则，不能仅因客户端失败就放宽类型边界。
- 成功身份进入既有持久安全缓存。
- 同一标题再次确认优先命中缓存，不重复调用服务端元数据接口。
- 真实探针中首次确认产生服务端请求，第二次确认新增请求为零，证明不会形成失败后的无限请求循环。

## V89：临时剧集身份与后补录迁移

### 临时身份

- TMDB 尚未收录的新剧可以建立 `local:tv:<标题规范化哈希>` 身份。
- 有豆瓣条目时保留 `douban:<subject_id>` 来源别名。
- 临时身份保存标题、别名、年份、`media_type=tv`、集数或连载证据、当前播放集数和线路绑定。

### 纳入边界

- 明确多集的电视剧、动漫、综艺、真人秀和脱口秀可以使用临时身份追更。
- 电影、独立视频和已知 0/1 集项目继续拒绝。
- 没有明确多集证据且存在电影冲突时，保持失败关闭。

### 身份迁移

- TMDB 后续收录后，将本地或豆瓣临时身份迁移为 `tmdb:tv:<id>`。
- 合并来源别名、播放记录、已看水位、当前续播、资源绑定和成功线路。
- 迁移后删除重复身份引用，不留下两个互相更新的追更条目。
- TMDB 空数据或空进度不能覆盖较新的本地进度。

## V90：选择性 TMDB 双链路热备

### 查询调度

- 冷查询立即启动客户端 TMDB 通道。
- 客户端超过约 800 毫秒未完成时，才并发启动 AList-TVBox 服务端通道。
- 客户端返回空结果、短期降级或本次传输失败时，也允许服务端补位。
- 任一通道先返回高可信电视剧结果时，可用于有明确多集证据的候选。

### 冲突裁决

- 内容类型不明确且客户端仍可用时，等待客户端电影冲突检查。
- 客户端已确认电影冲突时，服务端电视剧结果不能绕过拒绝。
- 服务端空数据不能覆盖客户端有效身份，TMDB 空进度不能覆盖本地播放进度。

### 通道健康

- 两条通道分别记录最近 8 次成功率和平均延迟。
- 连续失败 3 次后，对该通道短期降级 60 秒。
- 降级结束后允许自然恢复，不增加应用层无限重试。
- 成功身份进入现有缓存，后续查询不再固定双发。

### 公开晋升

- 根 `spiders_v2.json` 从 V70 升级到 V90。
- 公共插件 ID 保持 `douban_tmdb_follow_single`，用户重新导入原仓库地址即可升级。
- V70 仅保留在 Git 标签 `v70`，不再作为活动入口或运行时原子回退。

## V91：模块化开发线继续收敛

- 在 V90 封存基线上把前台缓存 SingleFlight、线路失败分类 TTL、预热排序、播放反馈、History 去重、刷新策略和预热淘汰审计迁移为可测试 Python owner。
- 运行时继续生成单个 Python 文件，未改为部署多模块目录。
- V91 的私有插件 ID 与公开插件 ID 保持隔离；V91 未作为独立公开版本发布。

## V92：资源身份验证与预热状态

### 搜索与详情分层

- PanSou 只承担资源召回、链接提取、去重、排序和可选可用性检查；这些结果不能直接证明影片身份。
- AList-TVBox 的资源名清洗、搜索标题传递和元数据补全用于增强证据，但同样不能单独完成身份确认。
- 白名单插件的搜索标题不足时可以进入详情验证，不再因弱标题在详情前被统一评分误杀。
- 禁用变体、错季和错年继续在搜索层硬拒绝，不因来源可信而放宽。
- 详情层必须由标题或 TMDB/豆瓣 ID 独立匹配，并拒绝电影/剧集类型、年份和季号冲突。
- 有限恢复 `百.花.杀`、`百 花 杀` 等中文拆点或拆空格形式，不做通用拼音、同音字或模糊猜测。

### 用户可见状态

- 追更卡片通过 `vod_remarks` 显示“已预热”“预热中”“已绑定资源”“预热线路待刷新”。
- 状态展示不修改图片 URL，也不使用会改变客户端导航语义的标签字段。

### 公开晋升

- 公共插件 ID 继续使用 `douban_tmdb_follow_single`，版本从 90 升到 92；用户重新导入原仓库地址即可刷新同一插件。
- 公共 History 默认规范键同步切换为公共插件 ID，同时保留 V80/V91 私有 ID 作为历史别名，便于重复记录合并。
- 公开单文件只由 V92 owner 构建结果替换公开身份元数据生成，不从手工修改的私有或公开产物反向维护。

## V92 公开产物

- 公共插件 ID：`douban_tmdb_follow_single`
- 公共版本：`92`
- 运行时文件：`py/豆瓣TMDB追更单入口.py`
- 公共源码：`1057488` 字节
- 公共源码 SHA256：`0044CD12C2AD0A1A62CC4FA8353EAAC2E883239F339C911928F1C5E648FCB088`
- 根索引：`230` 字节
- 根索引 SHA256：`28F6DB0DC1E639E0718F83AC5F8E9185E583890332A16D759D3C6BE7CD1F3B62`

## V93：自动识别与声明式 Web 配置

### 运行环境

- AList-TVBox raw 插件运行时通过 Atvp 内部模块名 `atvp_inner_spider` 自动识别。
- 用户不再填写 `atvp_plugin_mode`，容器生成的 `api`、`token`、`secret`、`loader`、`source` 和 `raw` 也不进入业务配置。
- 旧 EXT 中的 `atvp_plugin_mode=alist-tvbox-raw` 暂时保留兼容，但不再出现在表单或示例中。
- FongMi 直接 `.py` 且 EXT 为空时继续保持独立运行。

### 声明式表单

- 插件 Web 表单只展示 TMDB Token、同步服务地址、同步账号、同步密码和主动预热线路。
- Token 与密码使用密文输入，主动预热线路使用布尔开关。
- 独立拦截器表单只展示播放记录缓存、请求超时、TLS、标题、续播集和播放位置等过滤行为。
- 插件 schema 与拦截器 schema 均拒绝未声明的额外字段。

### 独立刷新链

- 追更助手插件和独立拦截器是两条部署记录；导入插件仓库不会自动刷新已有拦截器。
- 用户升级时先重新导入仓库，再在过滤器管理中点击“刷新拦截器”，保存订阅后刷新 FongMi/TvBox。
- 拦截器使用 `detail, player` 拦截点、`skip` 错误策略和 `exclude` 作用范围，并排除追更助手自身。

## V93 公开产物

- 公共插件 ID：`douban_tmdb_follow_single`
- 公共版本：`93`
- 运行时文件：`py/豆瓣TMDB追更单入口.py`
- 公共源码：`1062551` 字节
- 公共源码 SHA256：`8690A219BA9FD2D579C9CDFADAA94632815F8600F5EE1396A34015FAFA623043`
- 根索引：`230` 字节
- 根索引 SHA256：`724C65D6923550C3E95C9513B0555E2243422FC80846E9BC7E89FE0E8A70E9B0`

## V94：详情信任边界与云盘标签解析

### 候选准入

- `vod1`、`vod`、`telegram` 的弱标题候选仅可进入详情信任流程，不能直接成为可播或持久化的长期线路。
- 详情信任仍要求详情标题或外部 ID、类型/年份/季号、播放器、安全输出、Range 与目标集覆盖全部通过；任一验证失败即拒绝。
- PanSou 保持严格标题门禁，已有绑定也不能绕过该规则。
- 排序优先处理待详情验证的候选，再处理严格候选，避免低质量绑定遮蔽有效线路。

### 分集和容量

- 先识别分辨率标记，再从完整标签解析真实容量。
- `31 4K(3.75 GB)` 解析为第 31 集和 `3.75 GB`；`4K` 不会被识别为容量或集号。
- 没有明确分集号的纯分辨率标签继续使用调用方提供的默认集号。

## V94 公开产物

- 公共插件 ID：`douban_tmdb_follow_single`
- 公共版本：`94`
- 运行时文件：`py/豆瓣TMDB追更单入口.py`
- 公共源码：`1065579` 字节
- 公共源码 SHA256：`9A80B6A78331EDEBDC14E8E6929A2310D9DE6F3E0A18126223AC2B7C268C2B6F`
- 根索引：`230` 字节
- 根索引 SHA256：`CD0816F41FAE43CD12FD4045E1086EA89122770EB3302E7CF2B0A73651735D20`

## V95：PanSou 详情信任与身份回写

### 详情验证

- `vod1`、`vod`、`pansou` 和 `telegram` 的弱搜索标题可以进入有界的待详情验证队列，但不直接成为可播或长期绑定线路。
- PanSou 详情需要实际形成可播放资源后，才进入后续播放器、安全输出、媒体可达性和目标集覆盖验证。
- PanSou 搜索或详情携带的 TMDB、豆瓣、IMDb、TVDB 信息不作为追更身份依据；身份仍由当前追更条目与详情验证结果共同约束。

### 播放组和 ID 投影

- PanSou 组与普通资源组按来源独立保留，`vod_play_from`、`vod_play_url` 和 `vodFlags` 不会被折叠为同一组。
- 最终详情含 PanSou 播放组时，先清除资源端 `tmdb_id`、`douban_id`、`imdb_id`、`tvdb_id` 及其 camelCase 别名。
- 随后只回写当前追更条目的对应 ID；追更条目没有的字段保持缺失，不回退使用资源端值。

## V95 公开产物

- 公共插件 ID：`douban_tmdb_follow_single`
- 公共版本：`95`
- 运行时文件：`py/豆瓣TMDB追更单入口.py`
- 公共源码：`1066649` 字节
- 公共源码 SHA256：`5045ADAD813934F8C6FA8434C1E4D0D1E72CA522A6F4F2C4F3DD4FCA2997655C`
- 根索引：`230` 字节
- 根索引 SHA256：`09DF1AB361D3A79795CB877090EF0B3B41303C1430EE2C5DAE5ABF9D8F3EB30E`

## 验证边界

- V91/V92 owner 与播放追更回归：`148 passed`。
- PanSou/AList-TVBox 资源身份源码合同：`19/19 passed`。
- V93 运行时自动识别、声明式配置、私有构建和公共生成合同：`12 passed`。
- V94 资源信任、模块构建和公共发布合同：`11 passed in 525.40s`。
- V91-V94 完整回归：`113 passed in 435.90s`。
- V94 ATVP direct-play、FongMi 双运行时和 FongMi category contract 均通过。
- V95 PanSou 详情信任、独立播放组和追更身份回写回归：`14 passed in 198.82s`。
- V95 ATVP direct-play、FongMi 双运行时和 FongMi category contract 均通过。
- Python 编译、确定性构建、ATVP direct-play `20/20`、FongMi 双运行时 `25/25` 和敏感值扫描通过。
- 输入未变化的历史证据直接复用，没有重复运行旧十八步门禁、全量 pytest、Macro A/B 或 Chaos。
- 自动化结果不替代用户异地 FongMi 刷新订阅后的真实播放验收。
