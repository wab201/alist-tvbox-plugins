# 豆瓣 TMDB 追更助手架构介绍

本文只说明公开 V92 的结构和边界，不包含开发源码、构建规则、测试实现、私有部署参数或内部工程证据。

## 发布形态

插件在运行时保持单个 Python 文件，以同时满足：

- AList-TVBox raw Python 插件加载。
- FongMi/TvBox 直接 Python 站点调用。
- 固定插件 ID 和递增版本的仓库升级。
- 无需把开发工具链部署到用户环境。

公开仓库中的 Python 文件是发布产物，不是开发源。

## 逻辑模块

V90 的内部职责按以下边界组织：

| 中文模块 | 主要职责 |
| --- | --- |
| 版本元数据 | 插件身份、版本、运行模式和兼容声明 |
| 追更交互 | 追更确认、取消追更、同步反馈、主动预热和状态卡片 |
| 候选识别 | 电视剧/综艺识别、电影排除、TMDB/豆瓣/临时身份解析 |
| 播放记录合并 | 本地与云端 History、墓碑、完成状态和单调进度合并 |
| 线路预热与恢复 | 资源搜索、可达性验证、长期绑定、失效替换和续播恢复 |
| 播放列表输出 | 分组、排序、集数覆盖、播放 ID 和客户端输出 |
| 资源名规范化 | 清理年份、季集、画质、网盘和其他标题装饰 |
| 资源身份验证 | 分离搜索准入与详情确认，拒绝错季、错年、类型和外部身份冲突 |
| 追更状态投影 | 将预热、绑定和待刷新状态稳定写入卡片备注 |

## 主要数据流

```text
豆瓣 / TMDB / 收藏 / 播放记录
                 │
                 ▼
        剧集身份与追更状态
                 │
        ┌────────┴────────┐
        ▼                 ▼
 播放进度合并        资源候选搜索
        │                 │
        │          详情与播放安全验证
        │                 │
        └────────┬────────┘
                 ▼
        线路绑定与主动预热
                 │
                 ▼
       FongMi/TvBox 播放列表
```

## 身份和进度原则

- 优先使用正式 `tmdb:tv:*` 身份。
- TMDB 尚未收录时允许使用本地或豆瓣临时剧集身份。
- 后续身份迁移会合并来源、进度和线路，而不是创建两份互相竞争的状态。
- 较新的本地播放进度不会被 TMDB 空数据或较旧云端记录覆盖。
- 多集电视剧、动漫、综艺和真人秀可追更；电影、独立视频和已知单集项目拒绝。

## 资源和线路原则

- 候选可以来自 AList 搜索、PanSou、Telegram 和受控订阅插件。
- PanSou 的召回、链接提取、去重、排序和可用性检查不等于影片身份正确；AList-TVBox 的标题清洗和元数据补全也不能单独作为身份证明。
- 白名单插件返回的弱标题只允许进入详情验证；搜索层的错季、错年和禁用变体仍然硬拒绝。
- 搜索结果不能直接成为长期线路，必须经过详情标题或外部 ID、类型/年份/季号、播放器、安全输出、媒体可达性和目标集覆盖验证。
- 长期状态只保存可刷新资源身份、播放句柄、季集和质量摘要。
- Cookie、Token、签名 URL 和临时认证参数不进入长期绑定。
- 已验证线路优先复用，只有失效后才重新搜索。
- 预热和绑定状态只进入卡片备注，不改写图片 URL、分类动作或导航标签。

## TMDB 热备原则

- 冷查询先使用客户端通道。
- 客户端响应过慢、返回空结果、降级或传输失败时，服务端通道才补位。
- 高可信电视剧结果可以先返回，但类型不明确时仍检查电影冲突。
- 通道失败只触发短期降级，不形成无限重试。

## 公开与开发边界

- 公开仓库：运行时单文件、导入索引、用户文档、配置示例和发布记录。
- 非公开开发区：模块化 owner、构建器、测试、门禁、源码取证、私有部署和服务器报告。
- 开发修改先在非公开模块中完成验证，再生成唯一公开单文件；公开产物不反向成为开发源。
